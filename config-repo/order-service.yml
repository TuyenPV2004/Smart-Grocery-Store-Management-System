auth:
  service:
    base-url: ${AUTH_SERVICE_URL:http://auth-service}
catalog:
  service:
    base-url: ${CATALOG_SERVICE_URL:http://catalog-service}
inventory:
  service:
    base-url: ${INVENTORY_SERVICE_URL:http://inventory-service}
voucher:
  service:
    base-url: ${VOUCHER_SERVICE_URL:http://promotion-service}
